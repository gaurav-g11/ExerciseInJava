package exercise.oops

// instance-variable means state.

// method means behaviour.

// object reference is saved in stack memory.
// new Object saved in heap memory.
/*
Example: Human h = new Human();
h is a reference variable.It's saved in stack memory.
new Human is a object.It's saved in heap memory.
*/

//In Java does not support Copy Constructor.But we can create copy constructor like below example:
/*
public Human(Human old){

}
*/

// var-ads in java ?

//LinkedList :- Aunty(Head) -> Mohit(Node) -> Mohini(Node) -> Akshay(Node) -> Anushka(Node)